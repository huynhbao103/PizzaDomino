@model IEnumerable<PizzaDominos.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>QUẢN LÝ LOẠI HÀNG HÓA</h2>

<table class="table">
    <tr>
        <th>
            MÃ MÓN
        </th>
        <th>
            TÊN MÓN
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr class="btn-sm">
            <td>
                @Html.DisplayFor(modelItem => item.CatID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CateName)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.CatID }, new { @class = "btn btn-sm btn-success" }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.CatID }, new { @class = "btn btn-sm btn-danger" })
            </td>
        </tr>
    }

</table>
<p>
    @Html.ActionLink("Thêm mới", "Create", null, new { @class = "btn btn-primary" })
</p>
